{
  "name": "Mean",
  "version": "0.0.1",
  "description": "Basic setup of MEAN stack",
  "main": "server/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "deainya",
  "dependencies": {
    "express": "^4.14.0",
    "mongodb": "^5.0.0",
    "telegraf": "^3.6.0",
    "forever": "^4.0.0"
  }
}
